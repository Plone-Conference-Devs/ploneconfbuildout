[buildout]
parts =
    productdistros
    zopepy

# Change the number here to change the version of Plone being used
extends = 
    http://dist.plone.org/release/4.1b1/versions.cfg
    http://good-py.appspot.com/release/dexterity/1.0b1
    http://good-py.appspot.com/release/plone.app.caching/1.0a1
    getpaid-versions.cfg

versions = versions

find-links +=
    http://dist.plone.org/release/4.0b3
    http://dist.plone.org/thirdparty

eggs =
    netsight.ploneconf2010_theme
    netsight.conferenceregistration
    netsight.conferencetalks
    plone.app.dexterity
    getpaid.vatplus
    getpaid.paypal
    quintagroup.seoptimizer
    plone.app.caching
    gdata


extensions = mr.developer
auto-checkout = *
always-checkout = true
    
[sources]
getpaid.vatplus = fs getpaid.vatplus
getpaid.paypal  = fs getpaid.paypal
netsight.ploneconf2010_theme = git git@github.com:ploneconf/netsight.ploneconf2010_theme.git
netsight.conferenceregistration = git git@github.com:ploneconf/netsight.conferenceregistration.git
netsight.conferencetalks = git git@github.com:ploneconf/netsight.conferencetalks.git


[versions]
gdata = 2.0.9


# Use this section to download additional old-style products.
# List any number of URLs for product tarballs under URLs (separate
# with whitespace, or break over several lines, with subsequent lines
# indented). If any archives contain several products inside a top-level
# directory, list the archive file name (i.e. the last part of the URL,
# normally with a .tar.gz suffix or similar) under 'nested-packages'.
# If any archives extract to a product directory with a version suffix, list
# the archive name under 'version-suffix-packages'.
[productdistros]
# For more information on this step and configuration options see:
# http://pypi.python.org/pypi/plone.recipe.distros
recipe = plone.recipe.distros
urls =
nested-packages =
version-suffix-packages =

[instance-settings]
# For more information on this step and configuration options see:
# http://pypi.python.org/pypi/plone.recipe.zope2instance
the-recipe = plone.recipe.zope2instance
user = netsight:gahzohW0
http-address = 12801
#debug-mode = on
#verbose-security = on
blob-storage = ${buildout:directory}/var/blobstorage
# If you want Zope to know about any additional eggs, list them here.
# This should include any development eggs you listed in develop-eggs above,
# e.g. eggs = Plone my.package
eggs =
    Zope2
    Plone
    ${buildout:eggs}
    Products.PloneGetPaid

# If you want to register ZCML slugs for any packages, list them here.
# e.g. zcml = my.package my.other.package
zcml =
    netsight.ploneconf2010_theme
    netsight.ploneconf2010_registration-overrides
    getpaid.paypal
    getpaid.paypal-overrides
    getpaid.vatplus
    getpaid.vatplus-overrides
    quintagroup.seoptimizer

products =
    ${buildout:directory}/products
    ${productdistros:location}

[zopepy]
# For more information on this step and configuration options see:
# http://pypi.python.org/pypi/zc.recipe.egg
recipe = zc.recipe.egg
eggs = ${instance-settings:eggs}
interpreter = zopepy
scripts = zopepy


[zeoserver]
recipe = plone.recipe.zeoserver
zeo-address = meccano:12800
effective-user = zope
blob-storage = ${instance-settings:blob-storage}